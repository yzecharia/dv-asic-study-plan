# ── OS / Editor junk ─────────────────────────────────────────────
.DS_Store
._*
*.swp
*.swo
*~
*.bak
*.orig

# ── Python (ad-hoc scripts) ──────────────────────────────────────
__pycache__/
*.pyc

# ── Vivado / xsim artifacts ──────────────────────────────────────
xsim.dir/
.Xil/
*.jou
*.log
*.pb
*.wdb
xsim.covdb/
*.covinfo

# Negate: keep Iron-Rule PASS logs that live under sim/
!**/sim/*.log

# ── Simulation outputs ───────────────────────────────────────────
*.vcd
*.fst
*.ghw
*.out
*.vvp

# ── Synthesis / build temp ───────────────────────────────────────
.yosys_tmp/
build/
waves/

# ── Yosys schematic outputs (regen via run_yosys_rtl.sh) ─────────
build_rtl/
*.dot
# .svg/.json kept tracked only when explicitly added; build artifacts ignored
*.synth.json
*.netlist.json

# ── Obsidian (personal vault config + scratch) ───────────────────
.obsidian/
Untitled*.canvas
Untitled*.base
Untitled*.md
# Empty daily-note scratch at repo root (YYYY-MM-DD.md)
/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].md

# ── Project-specific ─────────────────────────────────────────────
week_03_coverage_assertions/learning/misc/mailbox_example.sv
week_04_uvm_architecture/learning/salemi_uvm_primer/ch11_uvm_tests/skeleton/
